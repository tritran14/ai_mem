# AI Memory - Weather MCP Server

A Model Context Protocol (MCP) server for weather data using the National Weather Service API.

## Features

- Get weather alerts for US states
- Get detailed weather forecasts for coordinates

## Usage

Run the MCP server:
```bash
poetry run weather-mcp
```

Or run directly with Python:
```bash
poetry run python src/weather.py
```